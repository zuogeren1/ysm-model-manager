fix: 全面修复剩余数据风险点

### Go 端（app_workshop.go）
- ExportWorkshopSitesJSONFile: os.WriteFile → atomicWrite
- ImportWorkshopSitesJSONFile: 新增站点数校验（2-100）
- ResetWorkshopConfigs: 备份后再重置 + atomicWrite
- MergeWorkshopCreatorsFromJSON: 新增导入数据校验（>=20条）
  合并后完整性校验（>=100条，不足则回滚不保存）
  同时同步 role 字段
- ReplaceWorkshopCreatorsFromJSON: 先校验（>=100条）再备份写入

### 前端（workshop-core.js）
- tryAutoMergeCommunity: 社区合并后改为按站点逐站保存
  （SaveWorkshopCreatorsBySite），不再全量覆写
