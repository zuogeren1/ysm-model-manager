# v1.4.3 vs v1.4.2 — 对比

## 文件变更

| 文件 | 改动类型 | 说明 |
|---|---|---|
| `go/ysm/header.go` | 修改 | `YSMHeader.HasFree` 字段；`scanHeader` 新增 `---` 退出条件；`AnalyzeYSMHeader` 新增 `hasTextHeader` 检测 |
| `go/sync/sync.go` | 修改 | `repoByHash` 改为 `map[string][]types.ModelEntry` 支持同哈希多文件 |
| `frontend/js/utils/summarize.js` | 修改 | `freeBadge` 条件从 `header.isYsm` 改为 `header.hasFree` |
| `frontend/js/components/app-preview/index.js` | 修改 | `hasRealSummary` 增加 `texWidth > 0` 条件 |
| `frontend/js/components/app-preview/tpl.js` | 修改 | 纹理分配显示简化 |
| `frontend/js/components/app-content/workshop-site-view.js` | 修改 | 预设搜索按钮 HTML 闭合修复 |
| `workshop_sites.json` | 修改 | 新增 B站/爱发电预设搜索 |
| `build/release/workshop_sites.json` | 修改 | 同步预设搜索 |
| `creators.json` | 修改 | 新增 7 位创作者 → 共 87 位 |
| `docs/release-notes/v1.4.3.md` | 新增 | 发版说明 |
| `docs/release-notes/v1.4.3-compare.md` | 新增 | 对比文件 |

## 关键修复

### YSM 头部扫描安全
```
修复前: scanHeader 读取 200 行 → 二进制数据被解析为 Tips/标签
修复后: 遇到 --- 分隔线停止 + hasTextHeader 预检
```

### 同步状态哈希去重
```
修复前: map[string]ModelEntry → 同哈希文件只保留最后一个
修复后: map[string][]ModelEntry → 同哈希文件全部保留
```

## 发版说明原文

[用户版](./v1.4.3.md)
