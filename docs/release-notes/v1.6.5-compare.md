# v1.6.5 — 创意工坊 UI 大翻新 + 创作者频道精简 + 全局样式统一

## 变更内容

### 创意工坊 UI 大翻新

- `community-site-view.js` -407 行，大幅精简重构
- `render.js` 渲染逻辑优化（+167/-167）
- `events.js` 事件绑定重构
- `download-queue.js` 下载队列适配新 UI

### 创作者频道精简

- `community-core.js` 核心逻辑简化
- `workshop-data.js` 新增 — 创作者数据层独立拆分
- `workshop-icons.js` 新增 — 创作者图标映射独立拆分
- `shared-styles.js` 新增 — 共享按钮样式基类

### 全局样式统一

- `content-css.js` +345 行 — CSS 变量体系完善，Shadow DOM 样式覆盖增强
- `preview-css.js` — 预览面板样式统一
- `sidebar-css.js` — 侧栏样式统一
- `app-tree-styles.js` — 树组件样式统一

### Wails 构建产物布局

- `frontend/build/` — Wails 构建配置规范化（icon/info/manifest）

## 影响范围

- **轻微**：创意工坊 UI 变化、创作者频道数据层拆分
- **无**：API/接口变更

## 文件清单

### 新增

| 文件                                                   | 说明             |
| ------------------------------------------------------ | ---------------- |
| `frontend/js/components/app-content/workshop-data.js`  | 创作者数据层     |
| `frontend/js/components/app-content/workshop-icons.js` | 创作者图标映射   |
| `frontend/js/css/shared-styles.js`                     | 共享按钮基类     |
| `frontend/build/windows/icon.ico`                      | Wails 图标       |
| `frontend/build/windows/info.json`                     | Wails 构建信息   |
| `frontend/build/windows/wails.exe.manifest`            | Wails 清单       |
| `frontend/frontend/wailsjs/runtime/`                   | Wails 运行时绑定 |

### 修改

| 文件                     | 变更                 |
| ------------------------ | -------------------- |
| `community-site-view.js` | -407 行，大幅精简    |
| `content-css.js`         | +345 行 CSS 变量体系 |
| `community-core.js`      | 核心逻辑精简         |
| `index.js`               | 微调                 |
| `tpl.js`                 | 模板调整             |
| `download-queue.js`      | 新 UI 适配           |
| `events.js`              | 事件绑定重构         |
| `render.js`              | 渲染逻辑优化         |
| `preview-css.js`         | 预览面板样式统一     |
| `sidebar-css.js`         | 侧栏样式统一         |
| `app-tree-styles.js`     | 树组件样式统一       |
