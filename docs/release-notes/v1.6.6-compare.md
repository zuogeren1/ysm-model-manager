# v1.6.6 — 下载队列重构 + 创意工坊渲染优化 + 下载流程加固

## 变更内容

### 下载队列重构

- `download-queue.js` +714/-368 大幅重构
- 下载进度防骗逻辑优化
- 队列状态管理重构
- 取消/错误处理增强

### Go 端下载流程加固

- `app_download.go` +11 行下载稳定性增强

### 创意工坊渲染优化

- `render.js` 渲染逻辑精简
- `events.js` 事件绑定微调
- `community-core.js` 核心逻辑优化
- `content-css.js` 样式微调

## 影响范围

- **中等**：下载队列内部逻辑重构，对外行为无变化
- **无**：API/接口变更

## 文件清单

| 文件 | 变更 |
|------|------|
| `app_download.go` | +11 下载流程加固 |
| `download-queue.js` | +714/-368 下载队列重构 |
| `render.js` | 渲染逻辑精简 |
| `events.js` | 事件绑定微调 |
| `community-core.js` | 核心逻辑优化 |
| `content-css.js` | 样式微调 |
